version: 0.2

phases:
  install:
    commands:
      - sudo apt-get update
      - sudo apt-get install -y jq zip unzip
      - cd e2e
      - npm install
  pre_build:
    commands:
      #Assume role
      - export ASSUME_ROLE_NAME=arn:aws:iam::431593652018:role/PASTASLOTHVULGAR
      - export TERRAGRUNT_IAM_ROLE=$ASSUME_ROLE_NAME
      - eval $(./utils/aws-cli-assumerole.sh -r $ASSUME_ROLE_NAME)
  build:
    commands:
      - export PRM_ENDPOINT="https://j4dbzo021j.execute-api.eu-west-2.amazonaws.com/test"
      - npm test    

