version: 0.1

phases:
  install:
    commands:
      - sudo apt-get update
      - sudo apt-get install zip
  build:
    commands:
      - zip  .
      - aws s3 cp walking-skeleton-spikes.zip s3://terraform-serverless-kc4/walking-skeleton-spikes.zip
